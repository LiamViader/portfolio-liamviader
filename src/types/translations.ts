export type Lang = "en" | "es";

export type Translations = {
    title: string;
    subtitle: string;
    ai: string;
    games: string;
    home: string;
    about: string;
    projects: string;
    contact: string;
};
